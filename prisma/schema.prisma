generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Digimon {

  id String @id @default(uuid()) @unique
  name String
  level String
  imgUrl String @unique
  @@map("digimons")
}